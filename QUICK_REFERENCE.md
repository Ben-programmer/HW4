# 🎯 快速參考卡

## 一行命令啟動

```bash
cd "/Users/benlu/Desktop/Applications/周杰倫種族分類PK" && ./run.sh
```

## 當前設定

### 分類資訊
| 分類 ID | 顯示名稱 | 訓練照片 | 測試照片 |
|---------|---------|---------|---------|
| cookieslu | Cookies 呂 | 8 張 | 3 張 |
| helu | 何呂 | 8 張 | 3 張 |
| jay | 周杰倫 | 8 張 | 3 張 |

**總計**: 24 張訓練照片 + 9 張測試照片

## 常用命令

### 安裝
```bash
./install.sh
```

### 測試
```bash
source venv/bin/activate
python face_recognition.py
```

### 啟動
```bash
source venv/bin/activate
streamlit run app.py
```

### 一鍵啟動
```bash
./run.sh
```

## 檔案位置

```
專案根目錄: /Users/benlu/Desktop/Applications/周杰倫種族分類PK
├── 訓練照片: photos/[cookieslu|helu|jay]/
├── 測試照片: test_photos/[cookieslu|helu|jay]/
├── 主程式: app.py
└── 辨識模組: face_recognition.py
```

## 修改設定

### 更改顯示名稱
編輯 `app.py` 第 12-16 行：
```python
CATEGORIES_DISPLAY = {
    'cookieslu': 'Cookies 呂',
    'helu': '何呂',
    'jay': '周杰倫',
}
```

### 調整辨識閾值
編輯 `face_recognition.py` 第 104 行：
```python
def recognize_face(self, image_path: str, threshold: float = 0.6):
    # 降低閾值 (如 0.5) = 更容易辨識但可能誤判
    # 提高閾值 (如 0.7) = 更嚴格但可能無法辨識
```

## 預期執行時間

| 階段 | 首次執行 | 後續執行 |
|------|---------|---------|
| 安裝套件 | 3-5 分鐘 | - |
| 下載模型 | 1-2 分鐘 | - |
| 載入模型 | 20 秒 | 20 秒 |
| 建立資料庫 | 1 分鐘 | 1 分鐘 |
| **總計** | **5-8 分鐘** | **1-2 分鐘** |

## 遊戲資訊

- **題目數**: 9 題
- **分類數**: 3 個
- **計分方式**: 答對得 1 分
- **獲勝條件**: 總分高者獲勝

## 網址

- **本機**: http://localhost:8501
- **網路**: http://[您的IP]:8501

## 故障排除

### 權限錯誤
```bash
chmod +x install.sh run.sh
```

### 找不到模組
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### 埠號被佔用
```bash
streamlit run app.py --server.port 8502
```

## 重要檔案

| 檔案 | 用途 |
|------|------|
| `PROJECT_STATUS.md` | 📋 完整狀態報告 |
| `CHECKLIST.md` | ✅ 啟動檢查清單 |
| `QUICKSTART.md` | 🚀 快速開始指南 |
| `DATA_INFO.md` | 📊 資料詳細說明 |
| `USAGE.md` | 📖 使用說明 |
| `EXAMPLES.md` | 💡 範例與建議 |

## 支援

- **原始專案**: [蔡炎龍老師的 AI-Demo](https://github.com/yenlung/AI-Demo)
- **InsightFace**: [GitHub](https://github.com/deepinsight/insightface)
- **Streamlit**: [官方文檔](https://docs.streamlit.io/)

## 快速測試

```bash
# 1. 進入專案目錄
cd "/Users/benlu/Desktop/Applications/周杰倫種族分類PK"

# 2. 一鍵啟動
./run.sh

# 3. 在瀏覽器中開啟 http://localhost:8501

# 4. 點擊「開始初始化」

# 5. 開始遊戲！
```

---

**提示**: 將此檔案加入書籤，方便隨時查閱！
